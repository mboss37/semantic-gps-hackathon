# Dependencies
node_modules/
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Next.js / build output
.next/
out/
dist/
build/

# Env
.env
.env.*
!.env.example

# IDE / OS
.DS_Store
.vscode/
.idea/
*.swo
*.swp
Thumbs.db

# Coverage / test
coverage/
*.log

# TypeScript / Next
*.tsbuildinfo
next-env.d.ts

# Misc
*.pem
.vercel

# Claude Code, ephemeral state + per-developer settings
.claude/state/*
!.claude/state/.gitkeep
.claude/settings.local.json
.claude/worktrees/

# Playwright MCP working dir (screenshots, console logs, page snapshots)
.playwright-mcp/

# Stray Playwright screenshots written to repo root during dev sessions.
# Subfolder PNGs (public/, etc.) stay tracked.
/*.png

# Private working notes — drafts NOT for public consumption (proto-SEP,
# MCP Discussion drafts, internal strategy memos). Anything under
# `_private/` stays local. Never push.
_private/
